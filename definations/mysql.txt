/*
========================================
MYSQL INTERVIEW QUESTIONS (1–20)
WITH SIMPLE EXPLANATIONS + QUERIES
========================================
*/

/*
1. WHAT IS MYSQL?
MySQL is a relational database management system (RDBMS).
It stores data in tables using rows and columns.
It uses SQL (Structured Query Language) to manage data.
*/
--Example:
SELECT "Hello MySQL";


/*
2. DATABASE vs TABLE
Database is a collection of tables.
Table stores actual data in rows and columns.
Each table has a structure defined by schema.
*/


/*
3. PRIMARY KEY
Primary key uniquely identifies each row.
It cannot be NULL and must be unique.
Used for indexing and relationships.
*/
CREATE TABLE users(
    id INT PRIMARY KEY,
    name VARCHAR(50)
);


/*
4. FOREIGN KEY
Foreign key creates relation between tables.
It references primary key of another table.
Used to maintain data integrity.
*/
CREATE TABLE orders(
    id INT,
    user_id INT,
    FOREIGN KEY(user_id) REFERENCES users(id)
    on delete cascade;
);

/*
5. INSERT DATA
Used to add new records into table.
*/
INSERT INTO users(id, name) VALUES(1, 'Harsha');


/*
6. SELECT QUERY
Used to fetch data from table.
*/
SELECT * FROM users;
select name from users;
select name , email from users;
selct name, count (*) from users group by name having count(*) > 1;



/*
7. WHERE CLAUSE
Used to filter records based on condition.
*/
SELECT * FROM users WHERE id = 1;


/*
8. UPDATE
Used to modify existing data.
*/
UPDATE users SET name = 'Ravi' WHERE id = 1;
update users set name = 'harsha vardhan' where id = 2;
update users set (name, email) = ('harsha vardhan', 'harsha@gmail.com') where id = 2;

/*
9. DELETE
Used to remove records from table.
*/
DELETE FROM users WHERE id = 1;

delete from orders where email = 2 and user_id = 2 and price > 100;

/*
10. LIMIT
Used to restrict number of rows returned.
*/
SELECT * FROM users LIMIT 5;


/*
11. ORDER BY
Used to sort results.
ASC → ascending, DESC → descending.
*/
SELECT * FROM users ORDER BY name ASC;
select * from users order by id desc;



/*
12. GROUP BY
Used to group similar data.
Often used with aggregate functions.
*/
SELECT name, COUNT(*) FROM users GROUP BY name;


/*
13. HAVING
Used to filter grouped data.
Works after GROUP BY.
*/
SELECT name, COUNT(*) 
FROM users 
GROUP BY name 
HAVING COUNT(*) > 1;


/*
14. JOINS
Used to combine data from multiple tables.
INNER JOIN returns matching records.
*/
SELECT users.name, orders.id
FROM users
INNER JOIN orders ON users.id = orders.user_id;


/*
15. LEFT JOIN
Returns all records from left table.
And matching records from right table.
*/
SELECT *
    FROM users
LEFT JOIN orders ON users.id = orders.user_id;


/*
16. INDEX
Index improves query performance.
Speeds up searching operations.
But increases storage.
*/
CREATE INDEX idx_name ON users(name);
create index idx_name on users(name);
select * from users where name like 'harsha';
select * from users where name like '%hars%';
select * from users where name like '_h%';
select * from users where name like '%h_%';
select * from users where name like '%h_r%;
select * from users where name not like 'harsha';


select * from users where name = 'harsha' or id = 10;

select * from users where name, id in ('harsha', 10); // wrong one 


select * from employees where role = 'dev' or role = 'tester' or role = 'hr';
select * from employees where role IN('dev', 'tester', 'hr');

select * from employees where age between 40 and 50;

select distinct name from employees;
select min(salary) from employees;


/*
17. NORMALIZATION
Process of organizing data to reduce redundancy.
Divides tables properly.
Improves data consistency.
*/


/*
18. TRANSACTIONS
Transaction ensures all queries run successfully.
If one fails, rollback happens.
*/
START TRANSACTION;
UPDATE users SET name = 'Test' WHERE id = 1;
ROLLBACK;


/*
19. VIEWS
View is a virtual table based on query.
Does not store actual data.
Used for security and simplicity.
*/
CREATE VIEW user_view AS
SELECT name FROM users;

create view view_name as select * from users;
then use like this => select * from view_name; 


/*
20. STORED PROCEDURE
Stored procedure is saved SQL logic.
Can be reused multiple times.
Improves performance.
*/
DELIMITER //
CREATE PROCEDURE GetUsers()
BEGIN
SELECT * FROM users;
END //
DELIMITER;

/*
========================================
END OF FILE
========================================
*/



delimeter //
create procedure GetUser()
BEGIN
    select * from usersl
END //

CALL GetUser();


select SUM(salary) from employees;
avg, max, min 
is null 
is not NULL

SELECT
COALESCE(phone,'No Phone')
FROM users;



1. Find Duplicate Emails
select email, count(*) from users group by email having count(*) > 1;

2. Second max salary in the employees
select MAX(salary) from employees 
    where MAX(salary) < select max(salary) from employees;

3. Show customers who have never placed an order.
    select u.* from users u
        left join orders o 
        on u.id = o.user_id
        where o.user_id IS NULL;

4. Calculate the total salary department-wise.
    select *, sum(salary) from employees group by department;

5. top 3 high paid employees
    select * from employees order by salary desc limit 3;

6. Find Employees Earning More Than the Average Salary
    select * from employees
        where salary > select *, avg(salary) from employees;

7. Count Employees in Each Department 
    select department,count(*) from employees group by department;

8. Find Employees Whose Name Starts with 'A'
    select * from employees where name like 'A%';

9. Find the Highest Salary in Each Department
    select *, max(salary) from employees group by department;

10. Find Employees Who Joined in 2025
    select * from employees where year(created_at) = 2025;
    select distinct department from employees;

11. Find the Third Highest Salary
    select * from employees;
    select max(salary) as top_1_salary from enployees
        where top_1_salary < (
            select max(salary) as top_2_salary from employees
        ) 
        where top_2_salary < (
            select max(salary) as from employees
        ); 

    select salary from employees order by salary DESC limit 1 offset 2;
    select distinct salary from employees order by salary DESC limit 1 offset 2;

12. Find Employees with Salary Between 30000 and 60000
    select * from employees where salary between 30000 and 60000

13. Find Employees Who Don't Have a Manager
    select * from employees where manager_id IS null;


14. Find Customers Who Placed More Than 5 Orders
    select user_id, count(*) from orders group by user_id having count(*) > 5;


15. Find Total Number of Orders for Each Customer
    select user_id, count(*) from orders group by user_id;


16. Find Employees Who Earn the Same Salary
    select salary, count(*) from employees group by salary having count(*) > 1;
    select salary, count(*) from employees group by salary having count(*) > 1;

17. find the latest employee Joined
    select * from employees order by id desc limit 1;
    select * from employees order by created_at limit 1;

18. Find all employees sorted by Name
    select * from employees order by name ASC;
    select * from employees order by name DESC;

19. find departments having more than 2 Employees
    select dep_id, count(*) from employees group by dep_id having count(*) > 2;

20. find employees whose salary is less than dept avg
    select * from employees e
        where salary < (
            select avg(salary)
            from employees
            where dep_id = e.dep_id
        );
